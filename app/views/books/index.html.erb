<% if @bookerror.present? %>
  <% if @bookerror.errors.any? %>
      <div id="error_explanation" class="alert alert-danger">
        <h2><%= pluralize(@bookerror.errors.count, "error") %> prohibited this user from being saved:</h2>
        <ul>
        <% @bookerror.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
<% end %>

<%= render 'users/template',  user: @user, book: @book %>
          

 <div class="col-lg-9">
  <table class="table table-striped">
   <h1>Books</h1>
 	<thead>
 	 <tr>
    <th></th>
    <th>Title</th>
 	   <th>Opinion</th>
      </tr>
 	</thead>

 	<tbody>
     <% @books.each do |book| %>
     <tr>
      <td>
     <span> 
      <%= attachment_image_tag book.user, :image, format: 'jpeg',fallback:"No_image.jpg",class: "iimg" %>
     </span>
   </td>
      <td>
      	<span><%= link_to book.title,book_path(book.id),class:"book_#{@book.id}" %></span>
      </td>
      <td>
      	<span><%= book.body %></span>
      </td>
     </tr>
    <% end %>
   </tbody>
  </table>
</div>
</div>
</div>
